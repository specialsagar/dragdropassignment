## Drag drop resize assignmet

### Requirements
- Node version 8 or later
- Node package manager
- Bower

### Installation
Go to root folder and enter the following commands 
- `npm install`
- `bower install`
- `npm start`

Then on your browser, enter the url - localhost:3000